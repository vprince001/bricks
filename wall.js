class RightWall {
  constructor(height, width, left, bottom, borderRadius) {
    this.height = height;
    this.width = width;
    this.left = left;
    this.bottom = bottom;
    this.borderRadius = borderRadius;
  }
}

class TopWall {
  constructor(height, width, left, bottom, borderRadius) {
    this.height = height;
    this.width = width;
    this.left = left;
    this.bottom = bottom;
    this.borderRadius = borderRadius;
  }
}

class LeftWall {
  constructor(height, width, left, bottom, borderRadius) {
    this.height = height;
    this.width = width;
    this.left = left;
    this.bottom = bottom;
    this.borderRadius = borderRadius;
  }
}

class BottomWall {
  constructor(height, width, left, bottom, borderRadius) {
    this.height = height;
    this.width = width;
    this.left = left;
    this.bottom = bottom;
    this.borderRadius = borderRadius;
  }
}
